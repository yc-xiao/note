1.创建线程池(线程安全，线程池为了提高效率，线程需要注意休眠)


1.需要线程池用于并发执行任务。
    新增，获取，查看任务。　
    启动，停止，继续，线程。
    因为线程是并发的，所以需要一个安全队列
    线程，任务

class ThreadPool(object):
    def __init__(self, max_size, *args, **kw):
        self.queue = Safe
